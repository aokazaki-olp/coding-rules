# MAINTAINING.md — 規約を編集するときの規律

この文書は**規約そのものを直す人**が読む。コードを書くときには読まない。

規約本体（`nodejs/CODING_RULES.md`）は常時ロードされる前提なので、**コードを書く読み手の行動を変えないものは本体に置かない**。編集規則はその典型で、ここに分離してある。

> **参照は一方向。** この文書から規約本体を参照してよいが、**規約本体からこの文書を参照しない**。
> 本体を読む人がここへ誘導されると、分離した意味が消える。

---

## 記述の3分類

規約に何かを書こうとするとき、まずどれに当たるかを決める。

| 分類 | 判定 | 扱い |
|---|---|---|
| **1. 書くべき** | 無いと規則が存在しない | 無条件で置く |
| **2. 書いてもいい** | 定義ではないが、**無いと読み手が「正しそうに見える別の行動」を取る** | 下記ゲートを通す |
| **3. 書くべきでない** | 正しいが、**読み手の行動を変えない** | 置かない |

**分類1には定義のほかに、成立条件・主語と適用範囲・他の箇所からの参照先が含まれる。** これらは例やコメントに埋まっていることがあり、見た目は分類2や3に見える。

**分類2と3を分ける実例**

| 記述 | 無かったら | 判定 |
|---|---|---|
| 「既定で DOM が混入し、Node プロジェクトなのに `document` が型を通る」 | `lib` を未指定のままにする | 2 |
| 「『推奨』水準では `!` を禁じるルールが含まれない」 | 推奨水準を選んでしまう | 2 |
| 「このランナーは追加依存が要らない」 | 何も変わらない（納得するだけ） | 3 |

---

## 追記するときのゲート（分類2の門）

本規約は常時ロードされる。**次の5つを全部満たさないものは書かない**（満たさないなら1行に圧縮するか、書かない）。

1. **再現性** — 本規約が既に与えている規則・例に従って踏んだ問題か（独自に組んだ構成の問題ではないか）
2. **普遍性** — §1〜§8 の主経路を辿る全員が踏むか（特定のオプション経路だけで踏むものではないか）
3. **恒久性** — 規則同士の構造的な相互作用か（今のツールのバージョン状況という時限的事実ではないか）
4. **プローズ充足性** — 一文で防げるか（正しさが手順の細部に依存し、文章では再現できないものではないか）
5. **頻度** — 繰り返し踏むか（一度きりの偶発ではないか）

**「正しいが行動を変えない行」が最大のコストである。** 検証の記録・変更履歴・網羅的な機能列挙・実行すれば分かること・モデルが既に知っていることは書かない。

**実装の詳細は規約に置かない。** 共有ユーティリティの中身・そのエッジケース処理の理由は、そのファイルのコメントに置く。規約が持つのは「いつそれを使うか」まで。

## 削るときの基準（分類1の保護）

ゲートは書き足すときの基準であり、**既にある行を削る基準が別に要る**。無いと文書は片方向に膨らむ。

**各行について「消したら、規約として何が定義されなくなるか」を問う。答えが無ければ削る。**

規則の**定義**と、その**根拠・機構の説明・実演**は別のものである。**規約が持つのは定義まで。**

| 削る対象 | 理由 |
|---|---|
| 規則を表・一文で定義済みのコード例 | 例は定義の再演にすぎない。**文では曖昧になるものだけ残す** |
| **要求と1対1で対応し、§8.3 の検証で同じことが分かる**機構の説明 | 検証義務があるので、壊れ方は各自が実測で知る |
| 同じ機構を複数箇所で説明したもの | 単一の節が持ち、他は参照だけにする（→「意図的に持たないもの」） |

**§1.2 が禁じる削除は対象外。** 規則の定義そのもの、および「機械が検出できるから」を理由とする削除はしない。ここで削るのは**定義ではなく、その再演と説明**である。

**「消す」と「狭める」を混同しない。** 適用範囲・対象・条件の変更は、見た目が削除に似ていても**実体は変更**であり、足す修正と同じ波及を生む。狭めるときは、**狭めた側に何が掛からなくなったかを列挙する**。

**例を消す前に、その例だけが与えている条件が無いかを確かめる。** 規則が表や一文で定義されていても、**成立条件が例のシグネチャやコメントにしか無い**ことがある。その例を参照している箇所も検索する。

## 意図的に持たないもの

以下は検討のうえで**持たない**と決めたもの。親切心で戻さないこと。戻すときは、なぜ前回外したのかを先に確認する。

| 持たないもの | 理由 |
|---|---|
| **設定ファイルの原本**（tsconfig / lint / 整形 / package.json の全文） | 本規約と一緒に検証されないので腐る。半年後にコピーした人に古い設定を配る害が、コピペできる利便より大きい。§8.1 の要求と §8.3 の検証義務で代替する |
| **ピン止めしたバージョン番号**（「TypeScript は 5.8 を使う」） | 最も速く腐り、しかも腐ったことに気づく機構が文書の内側に無い。満たすべき条件（§8.1 末尾）に変換し、固定値は `package.json` の隣に置く |
| **変更履歴・移行の経緯** | 常時ロードされる文書が持つ意味がない。git 履歴の仕事 |
| **「どれが機械強制されているか」の網羅表** | 検証手段を持たない文書がこれを掲げると、誤った安心を作る。§8.2 が持つのは**守備範囲の境目**（どこで機械が止まるか）であって、強制済みの網羅ではない。**「表に無いものは機械が守っている」と読ませない**ため、§8.2 は完全性を主張しない |
| **「どれが共通ルールか」の列挙**（§8.1） | 列挙は必ず実際のルール集合から遅れ、列挙外が共通でないという誤った含意を作る |
| **本文側での要件の要約** | 参照先が変わると要約だけが古いまま残る。要件の中身は単一の節が持ち、他は参照だけにする |
| **レビュー済み・検証済みの表示** | 読み手の行動を変えない。代わりに冒頭の非保証と §8.3 の検証義務を置く |

> **例外 — 下限マーカーは持ってよい。** 「この API は Node 26 以降」のような**機能の可用性の下限**は、ピン止めと違って腐らない（「Node 26 で入った」は永久に真）。§5.6 で新しい API を採るときは、この形で下限を書き残す（付録 A）。**「Node 26 を使う」は下限マーカーではなくピン止めなので書かない。**

---

